# Implementation Progress

| Field | Value |
|---|---|
| Assessed | 2026-09-26 |
| Against | `docs/SRS.md` v3.0 (requirement IDs below refer to it) |
| Plan | `docs/IMPLEMENTATION_PLAN.md` v3.0 |

Status labels: **Done** · **Partial** (useful code exists but doesn't meet the requirement yet) · **Replace** (works, but the v3 architecture replaces it) · **Retire** (no longer in scope) · **Not started**.

## Summary

| Area | Status | Reality |
|---|---|---|
| iOS tracking (`SightlineIOS/`) | Partial | Headless ARKit session sending FreeD over UDP. Good base for FR-TRK-001/005. The packet format and Euler conversion must be replaced with VCP + quaternions. |
| Blender extension (`BlenderAddOn/`) | Partial | Uses `vcam_native` only (task 1.3.1): a session starts on request, and a 60 Hz main-thread poll applies the newest canonical pose to the target camera and publishes STATUS. The Python FreeD path is deleted. No `VCam_Origin` rig or full N-panel yet (1.3.2/1.3.3). Manifest targets 5.2 and bundles the `vcam_native` wheel (macOS arm64 only so far). |
| Rust native module | Partial | `native/` workspace with 5 crates. `vcam-py` builds the PyO3 module `vcam_native` (only `version()` so far), which imports inside Blender 5.2.2 on macOS (task 0.1.4). |
| Viewfinder stream (Blender → iPhone) | Partial | 2.1a–d render offscreen at selectable resolution/fps/shading with an adaptive budget, apply Blender viewport colour management, normalize to sRGB/Rec.709 and hand one copied, colour-tagged frame to Rust. 2.2a encodes those frames to JPEG on a Rust worker thread, keeping their metadata. 2.2b specifies `VIDEO_FRAGMENT` with Rust fragmentation/reassembly; 2.2c1 sends encoded frames as fragments on the session UDP (`vcam-net` `VideoSender`), verified end to end with the fake iPhone. 2.2c2a exposes the encoder + sender to Python as `vcam_native.Session.start_video`, verified in headless Blender with the fake iPhone; libjpeg-turbo notices ship with the extension. Wiring into the add-on's stream loop (2.2c2b) and device playback (2.3) remain open. |
| C++ `legacy/DesktopReceiver/` (incl. CMIO extension) | Retire | Moved to `legacy/` in task 0.1.2 (ARC-006). Port the parsers, their tests, and the test-pattern generator to Rust, then delete the directory. |
| Repo / CI | Done | One git repo, private remote `origin`; loop PRs auto-merge on `ci-ok`. **CI green on 3 OSes:** PR #2 run `36096344645` (2026-09-25, head `01299b8`, merged as `bdc338e`): 15 of 15 jobs pass (rust-fmt, rust ×3, fuzz, wheels ×3, python, ios, extension, blender-smoke ×3, ci-ok). Getting there took three fixes on PR #2: NASM for turbojpeg-sys, the Rust 1.98 `as_chunks` lint, Windows `unused_mut` and a runtime-free iOS `malloc_logger` hook (`.github/workflows/ci.yml:49-57`, `native/vcam-fake-iphone/src/main.rs:148`, `SightlineIOS/SightlineIOSTests/AllocationCounter.swift:6-40`); a Windows-safe test scratch dir (`native/vcam-fake-iphone/tests/fake_iphone.rs:18-29`); and a lingering close after `ERROR` so Windows sends FIN, not RST (`native/vcam-net/src/control.rs:420-457`). |

**Maturity:** early prototype. Phase 0 tasks 0.1.1–0.1.6 are done, and the P0 CI gate is met: CI is green on 3 OSes (PR #2 run `36096344645`), and the extension with the Rust module installs and imports on all 3. Spikes S-1, S-2, S-3 have macOS records; Windows/Linux parts are owner-blocked. Phase 1: every plan task is done except 1.5.1c (needs a real iPhone); 1.3.6 (hold last good pose) was done in iteration 57. 1.4.2c's device reconnect still needs a real-network check on a device. Phase 2 may start early.

**Environment on the owner's Mac (2026-09-24):** Blender 5.2.2 LTS at `/Applications/Blender.app`; cargo/rustc 1.97.1; `.venv.nosync/` with pytest + maturin; Xcode 27.0 (via `DEVELOPER_DIR`) with the iOS 27.0 simulator.

## Last verified test runs (2026-09-24)

| Suite | Result | Note |
|---|---|---|
| Legacy C++ (`legacy/DesktopReceiver`) | 9/9 pass (fresh temp build dir) | Re-run after the move to `legacy/`. To be retired. The stale in-repo `build/` was deleted. |
| `BlenderAddOn/tests` (pytest, `.venv.nosync`) | 32 passed (2026-09-26, iteration 69) | Includes fps cap and mode-change pacing, hold vectors, status formatting, rig math and host ID persistence. Installed Blender also checks colour transforms, look changes and Display P3 normalization. |
| `native/` cargo fmt/clippy/test | 96 passed, 0 failed; fmt/clippy pass (2026-09-26, iteration 73) | All 5 crates. |
| Headless Blender `tests/blender/` (macOS arm64) | 10 scripts exited 0 (2026-09-26, iteration 69) | Fresh isolated extension. |
| S-1 `tests/bench_render.py` (macOS arm64) | Pass, 9/9 cells in each of 3 runs | Runs: headless as fast as possible (S-1a); headless at 30 fps and in the GUI with timers (S-1b). Numbers in SRS §13.1, JSON in `reports/s1-render-2026-09-24-macos-arm64*.json`. |
| S-2a `vcam-video` example `s2_jpeg` (macOS arm64) | Pass, 6 frames × 3 qualities × 2 encoders | Every JPEG decoded at the right size; PSNR 37.9–47.6 dB. Numbers in SRS §13.2 and `reports/s2-jpeg-2026-09-24-macos-arm64.txt`. |
| S-2b `vcam-video` example `s2_videotoolbox` (macOS arm64) | Pass, 3 res × 2 configs (RGBA input unsupported, reported) | 160/160 frames, 0 dropped; ffmpeg decoded all 3 streams without errors (Main, no B-frames). Numbers in SRS §13.2 and `reports/s2-videotoolbox-2026-09-24-macos-arm64.txt`. |
| S-2c `vcam-video` example `s2_openh264` (macOS arm64) | Pass, 3 res × 2 builds × 2 thread counts | 160 frames each; ffmpeg decoded all 6 streams without errors (Constrained Baseline, no B-frames). Numbers in SRS §13.2 and `reports/s2-openh264-2026-09-24-macos-arm64.txt`. |
| SightlineIOS `xcodebuild test` (iPhone 17 sim, iOS 27.0) | 60 tests, 4 skipped, 0 failures (2026-09-26, iteration 69) | Simulator unit and protocol tests passed; skipped cases require Release allocation mode or live device/Rust interop. No iOS code changed in 2.2c2a. |

---

## By SRS section

### §3 Architecture

| ID | Status | Evidence / gap |
|---|---|---|
| ARC-001 | Done | Extension + PyO3 crate `native/vcam-py` (`src/lib.rs`, maturin config in `pyproject.toml`), bundled as per-platform wheels. CI run `35958600428` built cp313 wheels for manylinux_2_28 x86-64, win_amd64, and macOS arm64, assembled them with `extension build --split-platforms`, and each platform zip imported `vcam_native` in Blender 5.2.2 (`blender-smoke` ×3). |
| ARC-002 | Partial | Crates split as required (`native/Cargo.toml:3-9`). `vcam-protocol` is `#![forbid(unsafe_code)]` (`native/vcam-protocol/src/lib.rs:5`), with no I/O. It now has the UDP framing/messages (task 1.1.3a). No net or production encoder code yet. |
| ARC-003 | Partial | Blender consumes only the canonical pose (vcp.md §6.1/§7): `BlenderAddOn/core/apply.py:34` builds the matrix from position + quaternion with no axis conversion, because ARKit→canonical happens once on the device (DM-002). Verified to within 6.3e-8 of the independently generated `testdata/motion/scripted.json` keyposes (`tests/blender/addon_apply.py`, task 1.3.1). iOS still sends FreeD Euler (1.4.2). |
| ARC-004 | Not started | iOS doesn't send a capture time or sequence number; Blender stamps receive time. |
| ARC-005 | Partial | Task 1.4.1. Swift 6 language mode for the app and test targets (`SightlineIOS/SightlineIOS.xcodeproj/project.pbxproj:250,269,423,456`), building with no errors or warnings. ARKit callbacks and UDP I/O are off the main actor: the `TrackingPipeline` actor runs on its own `DispatchSerialQueue` (`SightlineIOS/SightlineIOS/TrackingPipeline.swift:46`), which is also `ARSession.delegateQueue` (`TrackingSessionController.swift:38`) and the `NWConnection` queue, so frames and send completions enter the actor with no hop. The main actor gets snapshots at ≤ 15 Hz (`UIThrottle`, `TrackingPipeline.swift:17`) through a newest-wins `AsyncStream`. The `@Observable` controller replaces `ObservableObject`. Tests: `SightlineIOSTests/TrackingPipelineTests.swift` (60 Hz → exactly 15 Hz, off the main thread; ±4 ms jitter never over 15 per second; frames dropped after stop). Mutations caught: a mutable property on the delegate failed the Swift 6 build; removing the throttle and ignoring stop failed the tests. Not verified: the ARKit delegate-queue path on a device (the simulator has no ARKit); video decode off the main actor is Phase 3. |
| ARC-006 | Partial | Moved to `legacy/DesktopReceiver/`, with a retirement note at `legacy/DesktopReceiver/README.md:1-6`. Porting to Rust and deleting the directory are still open. |
| ARC-007 | Not started | Optional. |

### §4 iOS app

| ID | Status | Evidence / gap |
|---|---|---|
| FR-TRK-001 | Partial | Task 1.4.2a. One VCP `POSE` per `ARFrame` (`worldAlignment = .gravity`): `seq` from 1 per run, `capture_time_ns` = `ARFrame.timestamp` in ns, canonical position + quaternion via `VCPCoordinates` (`SightlineIOS/SightlineIOS/TrackingPipeline.swift:184-198`). Sealed with the session's device key and sent over UDP when a session exists. The first frame of vcp.md §6.1's example arrives on a loopback socket as `pose_normal.bin` (authenticated by the host key; header, seq, time, position and state exact; the quaternion within 1 ulp) (`SightlineIOSTests/TrackingPipelineTests.swift:198`). The session comes from the TCP client (task 1.4.2b): each run opens one (`SightlineIOS/SightlineIOS/TrackingSessionController.swift:113`), so `seq` restarts with the session as §8 requires, and poses go to the TCP peer's address and the challenge's UDP port (`VCP/VCPSessionClient.swift:289`); a live Rust host received 60 then 30 poses (x = 0.30 m, state 5) over two sessions (`SightlineIOSTests/VCPSessionClientTests.swift:338`, opt-in). Missing: the stored pairing that turns this on in the app (pairing UI and Keychain, 1.4.3b); a device run; O-1 (which clock `ARFrame.timestamp` uses; the SDK header doesn't say, so it needs a device check). |
| FR-TRK-002 | Done | The device side is done (task 1.4.2a): every `POSE` carries the vcp.md §6.1 `tracking_state` code taken from its own `ARFrame` (`SightlineIOS/SightlineIOS/VCP/VCPTrackingState.swift:13`, table test in `TrackingPipelineTests`), and the UI shows the state. Host hold done (task 1.3.6): the scene option **Hold Last Good Pose**, on by default (`BlenderAddOn/properties/scene_props.py:47`, N-panel `ui/panels.py:46`). While the newest pose isn't normal, `PoseHold` (`core/rig.py:161`) keeps the camera at the session's last normal pose, or leaves it alone before the first one; the next normal pose resumes. Controls still apply to the held pose, and STATUS `applied_pose_seq` stays at the held pose's seq (`core/apply.py:176`, `:186`). The panel shows "Holding last good pose (<reason>)" (`ui/panels.py:76`, `core/status.py:26`; checked visually in a GUI run, iteration 57). Vectors `testdata/rig/hold.json` (5 sequences, `tools/gen_testdata.py:812`) → `BlenderAddOn/tests/test_rig.py:55`. Over the wire, `vcam-fake-iphone --limited 120-210` (`native/vcam-fake-iphone/src/main.rs:112`) sends `limited(.relocalizing)` over the tilt move: with hold on the camera stays on the pan keypose on every tick and resumes at dolly/crane; with it off the tilt keypose is applied (`tests/blender/addon_apply.py:146`, `:182`, stub checks `:230`). Not checked: limited states from a real ARKit session (device, owner). |
| FR-TRK-003 | Partial | Host side of Set origin done (task 1.3.2a): any change of `origin_epoch` after the first one seen in a session (the shared `freshness.json` rule) re-zeros position and heading to the current pose (`BlenderAddOn/core/rig.py:62`, `:70`). The zero is stored on `VCam_Origin` (`core/apply.py:110`, `:124`), so it survives reconnects and saves. Verified over the real wire with the fake iPhone's `--set-origin-at` (task 1.3.2b). iOS button done (task 1.4.4a): **Set Origin** (`SightlineIOS/SightlineIOS/SettingsView.swift:76`, and the status screen's Origin rail button `ContentView.swift:93`, both enabled while tracking) bumps `origin_epoch` with wrap (`TrackingPipeline.swift:40`), sent as the next `CONTROL_STATE` (`TrackingPipelineTests.swift:284`). Still missing: a live device ↔ Blender run, which needs pairing (1.1.4b/1.4.2b, owner's `swift-srp` decision) and a device. |
| FR-TRK-004 | Partial | Task 1.4.4b. Every run enables horizontal + vertical plane detection, and LiDAR scene reconstruction (`.mesh`) only where `ARWorldTrackingConfiguration.supportsSceneReconstruction(.mesh)` reports it (`SightlineIOS/SightlineIOS/SceneUnderstanding.swift:12-18`, `:25-26`), still gravity-aligned; used by `startTracking` (`TrackingSessionController.swift:87`) and shown as "Scene understanding" while tracking (`SettingsView.swift:67`). Tests: `SightlineIOSTests/SceneUnderstandingTests.swift:6`, `:18`, `:24`. Missing: a device run to confirm the LiDAR session starts and to measure its stability and thermal effect (device, owner). |
| FR-TRK-005 | Done | `handleScenePhase` stops tracking on inactive/background. |
| FR-TRK-006/007 | Not started | |
| FR-TRK-008 | Done | Headless `ARSession`; no camera preview. |
| FR-VF-* | Not started | |
| FR-CTL-004 | Partial | Host side done (task 1.3.2a). `CONTROL_STATE` motion scale and locks (height, roll, pan-only) are merged per vcp.md §6.2 (`BlenderAddOn/core/rig.py:124`; absent fields kept, stale `state_seq` ignored) and applied in the rig step (`rig.py:109`), with `control_ack` echoed in STATUS (`core/apply.py:150`). Checked against `testdata/rig/rig_cases.json` (pytest) and through real Blender objects (`tests/blender/addon_apply.py`). Over the real wire (task 1.3.2b), `vcam-fake-iphone --scale 2 --locks 1 --set-origin-at 105` into the add-on in headless Blender matches the `scripted_*` rig vectors to within 6e-8, with `control_ack=2` seen by the device (`tests/blender/addon_apply.py`). iOS side done (task 1.4.4a): Rig section with motion-scale presets 1:1/2/5/10 plus a validated custom 1:N (0.001–1000) and the three lock toggles (`SightlineIOS/SightlineIOS/SettingsView.swift:75`); `DeviceControls` (`TrackingPipeline.swift:27`) goes out as the complete v1 state, `state_seq` 1 at run start and +1 per real change (`:164`, `:217`), repeated every 500 ms until an authentic, fresh `STATUS.control_ack` covers it (`:233`, `:248`; STATUS read on the same UDP connection, `UDPSender.swift:84`). Byte-exact against `control_state_full` (`TrackingPipelineTests.swift:249`). Still missing: sending for real needs a session endpoint (1.4.2b, blocked on `swift-srp`). |
| FR-CTL-001..003, 005..009 | Not started | |
| FR-UX-001/002 | Done | Task 1.4.3a discovers and sorts DNS-SD `_vcam-ctl._tcp` hosts by machine and `.blend` file (`SightlineIOS/SightlineIOS/HostDiscovery.swift:44,72-80`). Task 1.4.3b makes compatible rows selectable and retains manual address entry (`SettingsView.swift:25-69`), connects to the selected Bonjour service and uses its resolved TCP peer for UDP (`VCP/VCPSessionClient.swift:80-83,142-159,336-345`; `SightlineIOSTests/HostDiscoveryTests.swift:119`). The six-digit N-panel code runs SRP pairing then a session on the same TCP connection (`TrackingSessionController.swift:120-164`). Pairings are remembered per selected service or manual host:port in ThisDeviceOnly Keychain records; the challenge's authenticated `host_id` must still match (`PairingStore.swift:6-45`; `PairingStoreTests.swift:5-30`). The UI explains iOS/macOS local-network and Windows firewall prompts (C-4, `SettingsView.swift:72-74`). Simulator suite 49 passed, 3 skipped; physical-device Wi-Fi pairing remains owner verification. |
| FR-UX-003/004 | Partial | Task 1.4.5. **FR-UX-003 done:** the iPhone app is landscape-only (`project.pbxproj:413`, `:447`; iPad keeps all orientations for multitasking). The root view is a status screen (`SightlineIOS/SightlineIOS/ContentView.swift:14`): a black frame area for the future viewfinder, a status strip on the top edge and a one-handed control rail (Start/Stop, Origin, Settings) on the trailing edge, placed by `HUDLayout` (`StatusHUD.swift:93`), which caps both so they never enter the middle half of the frame. While tracking the rail auto-hides 4 s after the last use and a tap on the frame toggles it (`ChromeVisibility`, `StatusHUD.swift:56`, `:66`); while idle it stays up. The former form is the Settings sheet (`SettingsView.swift`, `ContentView.swift:53`). Status shows tracking state, pose rate from `seq` over capture time (`PoseRateMeter`, `StatusHUD.swift:7`; `TrackingSessionController.swift:162`), connection, and thermal. **FR-UX-004 partial:** thermal state shown and kept current from `ProcessInfo.thermalStateDidChangeNotification` (`TrackingSessionController.swift:68`), orange from `.serious` (`StatusHUD.swift:49`, `ContentView.swift:69`). The automatic stream resolution/fps reduction waits for the video stream (Phase 2). Tests: `SightlineIOSTests/StatusHUDTests.swift` (6). Not verified on a device (landscape ARKit axes are open item O-2). |
| Pose maths | Replace | `TrackingPose.swift` uses an aerospace Euler extraction on ARKit's Y-up frame. The replacement exists: `SightlineIOS/SightlineIOS/VCP/VCPCoordinates.swift` (DM-002 quaternion conversion, task 1.1.4a). Wiring it into the tracking path is task 1.4.2. |
| Project settings | Partial | Deployment target iOS 26.4; `SWIFT_VERSION = 5.0`; the camera usage string contains zero-width spaces (U+200B). |

### §5 Blender extension

| ID | Status | Evidence / gap |
|---|---|---|
| FR-BL-001 | Done | `blender_version_min = "5.2.0"`, `network` permission, `wheels`/`platforms`. CI writes all three platforms into the manifest (`tools/set_manifest_wheels.py`) and produced `windows-x64`, `linux-x64`, and `macos-arm64` zips that install and import (run `35958600428`). The committed manifest lists macOS only, for local builds. `windows-arm64` (SHOULD) not built. |
| FR-BL-002 | Partial | The native module owns the sockets and threads (`vcam-net` `ControlServer`/`UdpReceiver`, tasks 1.2.1–1.2.4). They are now exposed to Python as `vcam_native.Session` (`native/vcam-py/src/lib.rs:82`, task 1.2.5a): `start`/`stop` release the GIL; `latest_pose`, `stats`, `poll_event` and `pairing_code` are non-blocking reads of latest-sample slots and the event queue (C-2). Verified in headless Blender 5.2.2 (`tests/blender/session_native.py`). The add-on starts it on request and stops it in `unregister()` (`BlenderAddOn/core/session.py:110`, `:130`, `:229`; `tests/blender/addon_session.py`, task 1.2.5b). The add-on's only transport is `vcam_native` (task 1.3.1): `core/session.py:215` applies each new pose via `core/apply.py:150`; the Python FreeD client is deleted. |
| FR-BL-003 | Partial | Rig done on the host (task 1.3.2a). `VCam_Origin` (reused if the user placed it, otherwise created at the camera's position) → the target camera, parented with an identity inverse (`BlenderAddOn/core/apply.py:90`). The camera's `matrix_basis` is the local pose: the device pose relative to the Set-origin zero, locks, then motion scale (`core/rig.py:109`). The incoming pose is never edited. Verified in headless Blender with the fake iPhone under a translated/rotated/scaled user rig: local error 1.8e-7, and world = origin × local (`tests/blender/addon_apply.py`). Scripted controls over the wire are verified too (task 1.3.2b). Still to do: the N-panel rig controls (1.3.3), and robustness to deleted/renamed objects (1.3.4). |
| FR-BL-004 | Done | N-panel (`BlenderAddOn/ui/panels.py`, task 1.3.3), with every item FR-BL-004 lists:
  - session start/stop;
  - pairing code, grouped "045 479", with Pair/Cancel (`operators/session.py`);
  - connected device, tracking state (vcp.md §6.1 names), pose rate, loss;
  - pose-leg latency: host clock − (capture − clock offset), computed at apply (`core/status.py`, `core/session.py:252`), plus clock jitter; a Save Latency Report button once a device session has samples (`ui/panels.py:80`, task 1.5.1);
  - camera picker, smoothing toggle;
  - rig: motion scale "1:10" and locks from the device, shown read-only because the device owns CONTROL_STATE (FR-CTL-009), with host Set/Clear Origin.

  It redraws at 4 Hz from the session poll. Behaviour is tested in headless Blender with the fake iPhone (`tests/blender/addon_panel.py`; 3 mutations caught) and in pytest (`test_status.py`). The drawing was checked visually in a GUI Blender run (iteration 35: pairing and streaming screenshots; iteration 44: the Save Latency Report button while streaming and after the device left). |
| FR-BL-005 | Not started | The FreeD zoom/focus mapping was deleted with the FreeD path (task 1.3.1). Lens controls arrive as T2 `CONTROL_STATE` fields (O-4). |
| FR-BL-006 | Partial | Native side done (task 1.2.6). The One-Euro filter (`native/vcam-net/src/smooth.rs:93-139`) runs position per axis and orientation by hemisphere-corrected slerp, with speed-adaptive cutoffs. dt comes from the device `capture_time_ns`, so network jitter doesn't change the filtering. A gap over 0.5 s, a non-increasing capture time, or a tracking-state change restarts the filter at the raw sample. Per-receiver toggle: `UdpReceiver`/`ControlServer::set_smoothing` (`udp.rs:453`, `control.rs:252`). The setting survives session changes, and the filter state resets per session and on every change. Raw is always kept: `PoseSample::pose` is raw and `::smoothed` is applied. Python: `Session.set_smoothing`/`smoothing()` and `latest_pose()["smoothed_position"/"smoothed_orientation"]` (`native/vcam-py/src/lib.rs:268`, `:320`). Tests: 8 filter unit tests (formula check, jitter < 10 % variance, adaptive lag for position and rotation, short-way quaternion, restarts, validation), a loopback test (`native/vcam-net/tests/udp_receiver.rs:169`), and Blender `tests/blender/session_native.py`; 9 mutations caught. Default parameters aren't tuned on a device yet. The add-on applies the smoothed pose (1.3.1/1.3.2), and the N-panel has a Smoothing toggle that applies to the running session and to later sessions (scene property, task 1.3.3; `tests/blender/addon_panel.py`). |
| FR-BL-007 | Done | Task 1.3.4. Nothing in the add-on keeps `bpy` data across ticks (reload and undo free it, `ReferenceError` otherwise). Persistent `load_post`/`undo_post`/`redo_post` handlers (`BlenderAddOn/core/session.py:264-287`) re-apply the live pose after undo or reload; on reload the running session and its timer continue, and the new file's smoothing and name are taken. The camera choice is checked every tick (`core/apply.py:53`): a camera deleted in the UI (it lingers outside the scene while the target pointer uses it, and `scene.camera` still names it) is not driven and not replaced by another camera; the device gets STATUS error 1 (no camera) and the N-panel shows a warning (`ui/panels.py:41`). A renamed camera keeps being driven and STATUS carries the new name; a renamed rig is still found through its `vcam_origin` marker (`core/apply.py:77`). Tested in headless Blender with the fake iPhone (`tests/blender/addon_robust.py`: camera and rig rename, undo, save + reopen, UI delete, replacement camera, empty file; the poll must record no error); 6 mutations, 5 caught directly and the 6th (rig found by name only) caught once its fallback was also removed. GUI screenshot (iteration 36): the warning `"Camera" was deleted` under the camera picker. |
| FR-REN-001/003..004 | Partial | 2.1a: `BlenderAddOn/core/render.py:103-126` draws offscreen, reads one tick behind and hands the buffer to `vcam_native.FrameSlot.submit` (one Python-side copy). 2.1b: `core/session.py:234-256` drives it after pose apply, using host-clock time and applied/held seq; `core/render.py:29-55` paces GPU work by measured draw + read. Pending frames are dropped on camera loss, disconnect, undo/reload and mode changes. Worker encoding (2.2) remains open. |
| FR-REN-002 | Done | 2.1c: scene-persistent 640×360 through 1920×1080, 24/30/60 fps and Solid/Material Preview/Rendered (EEVEE) (`BlenderAddOn/properties/scene_props.py:61-81`). The timer applies changes immediately, frees the old GPU buffer and drops its pending frame (`core/session.py:247-256`, `core/render.py:94-100,160-170`); `ui/panels.py:47-56` warns that EEVEE can lag the UI and drop fps. Headless Blender tests compare Material/EEVEE pixels to independent draws and check live dimensions, frame hand-off and saved-file persistence (`tests/blender/render_offscreen.py:119-131`, `render_session.py:65-120`). Blender GUI panel warning seen at 720p/60 fps; 3 mutations caught. |
| FR-REN-005 | Partial | Renderer sub-step 2.1d done: `core/render.py:129-154` uses Blender viewport colour management, temporarily selects sRGB and updates the evaluated scene, then restores display settings. Raw frames carry `FrameColorSpace::SrgbRec709` (`native/vcam-video/src/frame.rs:18-32,56-89`), exposed at `native/vcam-py/src/lib.rs:125`. Installed Blender compares look/view changes and wide-gamut conversion to independent draws (`tests/blender/render_offscreen.py:136-187`), with two caught mutations and a visually inspected readback. Full stream colour/profile propagation through encoding and playback remains 2.2/2.3; Solid follows Blender Workbench behaviour (SRS §13.1 note). |
| FR-TAKE-* | Not started | |
| UI text | Done | "Live Link" removed (task 0.1.6). The FreeD operators were deleted in 1.3.1; the add-on now offers "Start Sightline Session" / "Stop Sightline Session" (renamed from VCam, 2026-09-25) (`BlenderAddOn/operators/session.py`). |

### §6 Protocol

| ID | Status | Evidence / gap |
|---|---|---|
| DM-001..003 | Partial | Specified (`vcp.md` §6.1, §7). Rust pose type (`vcam-protocol`); Swift pose type and ARKit→canonical conversion (`SightlineIOS/SightlineIOS/VCP/VCPMessages.swift`, `VCPCoordinates.swift`, task 1.1.4a), no Euler angles. Not yet used by the live tracking path (1.4.2). Open items O-1/O-2 (device checks). |
| DM-004 | Partial | `testdata/coords/arkit_to_canonical.json` (9 cases) is consumed by Swift `VCPGoldenTests.testARKitToCanonicalMatchesDM004Vectors` (position, quaternion, `matrix_world` columns, view direction; task 1.1.4a). The Rust side has no ARKit conversion by design (DM-002); the Blender apply step (1.3.2) will use the canonical columns. |
| PR-004 | Done | Spec with byte layouts and example hex (`docs/protocol/vcp.md`) plus golden vectors in `testdata/vcp/`: `messages.json` + 10 `.bin`, `receive.json` (26 receive-rule cases), `freshness.json`, `pairing.json`, `session.json`, `srp-rfc5054-appendix-b.json`. Generated by `tools/gen_testdata.py`, which self-checks against RFC 5054 App. B, RFC 5869 A.1, and every `vcp.md` example (7, including the §6.5 `VIDEO_FRAGMENT` example, whose vectors live in `testdata/video/`, task 2.2b). |
| PR-001..003, PR-006 | Partial | Rust (`native/vcam-protocol`): UDP framing, HMAC trailer, messages, TCP control frames, SRP-6a pairing, session keys (1.1.3a/b). Swift (`SightlineIOS/SightlineIOS/VCP/VCPEndpoint.swift`, `VCPMessages.swift`, 1.1.4a): UDP framing, CryptoKit HMAC-SHA256/64 with constant-time compare, receive rules, POSE/CONTROL_STATE/CLOCK/STATUS; byte-exact golden vectors. Host networking authenticates incoming pose/control and sends session-scoped STATUS/CLOCK (`native/vcam-net/src/udp.rs:218`, 1.2.2b.2b); wire tests `native/vcam-net/tests/udp_receiver.rs:209`, `:246`, `:313`. Applied acknowledgements are published through `ControlServer::update_status` (`control.rs:246`), never inferred from reception. Swift TCP control frames and pairing (task 1.1.4b): `VCPControlMessage` encode/decode/`frameLength` for HELLO, PAIR_*, SESSION_*, ERROR (`SightlineIOS/SightlineIOS/VCP/VCPControl.swift:115`, `:160`, `:171`); device SRP-6a on `swift-srp` 2.4.0 (`VCPPairing.swift:29`, `:72`), M2 check and PK (`:113`), session proofs and keys (`:133`, `:151`); every TCP vector frame byte-exact and malformed frames rejected (`SightlineIOSTests/VCPGoldenTests.swift:184`, `:216`). Device TCP client (task 1.4.2b): `VCPControlChannel` (`NWConnection`, whole frames, `ERROR` → `.host`, one handshake deadline; `SightlineIOS/SightlineIOS/VCP/VCPSessionClient.swift:83`, `:104`, `:122`), `VCPSessionClient.pair`/`startSession`/`connect` (`:264`, `:289`, `:326`); `pairing.json` and `session.json` replayed over loopback TCP byte-exact (`SightlineIOSTests/VCPSessionClientTests.swift:290`, `:137`), and a live pair → session → POSE/CONTROL_STATE/STATUS exchange with the Rust host (`:338`, opt-in). |
| PR-005 | Partial | `Endpoint::open` applies vcp.md §4.3 steps 1–8 in order with typed `DropReason`s. Decoding is bounds-checked (`src/wire.rs`), with no `unwrap` (clippy-enforced). It matches all 26 `receive.json` verdicts. Fuzz targets `native/fuzz/fuzz_targets/{udp_open,control_decode,pairing_inputs}.rs` (task 1.1.3c) and `video_reassembly.rs` (task 2.2b; VIDEO_FRAGMENT decode, re-seal round trip, reassembly: 4.87M execs in a 60 s local nightly `cargo fuzz` run, 0 crashes) assert round-trip and no-forgery properties. So far they've only had a **coverage-blind** 60 s run on stable (274.5M / 279.9M / 45k execs, 0 crashes). A coverage-guided `cargo fuzz` run (nightly) is pending CI or the owner's OK for a local nightly toolchain. |
| PR-FD-001 | Replace | Blender side removed (task 1.3.1: `core/freed_parser.py`, `udp_client.py`, `transform.py`, `operators/tracking_receiver.py`, their tests and `tests/freed_test_sender.py` deleted; kept in git history). The iOS side is removed too (task 1.4.2a): `FreeDPacketEncoder.swift`, the Euler `TrackingPose.swift` and `FreeDPacketEncoderTests.swift` are deleted, and the app builds VCP `POSE` instead. Still to do: `legacy/DesktopReceiver/src/protocol/FreeDParser.cpp` (ARC-006 retirement). |
| PR-FD-002, PR-OTIO-001 | Not started | Optional, T4. |

### §7 Cross-platform

| ID | Status | Evidence / gap |
|---|---|---|
| XP-001 | Done | CI run `35958600428`: `wheels` (manylinux 2_28, Windows, macOS arm64) and `extension` (`--split-platforms`) green. |
| XP-002 | Done | CI `blender-smoke` builds the fake iPhone (`cargo build --release --locked -p vcam-fake-iphone`, `.github/workflows/ci.yml:176-181`) and runs all seven headless scripts on Linux, Windows and macOS (`ci.yml:220-230`), among them the integration test `tests/blender/addon_apply.py` (camera `matrix_basis` and `matrix_world` at all 5 scripted keyposes under a translated/rotated/scaled rig, plus scripted controls over the wire; task 1.3.5). PR #2 run `36096344645`: each of the 3 jobs logged `VCAM_NATIVE_OK`, `VCAM_SESSION_OK`, `VCAM_ADDON_SESSION_OK`, `VCAM_ADDON_APPLY_OK`, `VCAM_ADDON_PANEL_OK`, `VCAM_ADDON_ROBUST_OK` and `VCAM_POSE_LEG_OK`. |
| XP-003 | Not started | S-2 found that the recommended encoders link statically (turbojpeg) or are OS frameworks; no production dependency yet. |
| XP-004 | Not started | S-3a (SRS §13.3): the linker-ad-hoc-signed `.so` loads when installed from a quarantined zip through Blender. A quarantined `.so` can hit a Gatekeeper prompt and denial, so Developer ID signing + notarization is still required (owner credentials). |
| XP-005/006 | Not started | |

### §8–§10

| ID | Status | Evidence / gap |
|---|---|---|
| NET-001 | Partial | Pure-Rust dual DNS-SD via pinned mdns-sd 0.21.4 (`native/vcam-net/src/discovery.rs:19`, `:78`, task 1.2.3): actual TCP/UDP ports, vcp/blend/host/tcp/udp TXT, bind-scoped addresses, updates and stop/drop withdrawal. `ControlServer::advertise`/`discovery_error` (`control.rs:203`, `:226`) own the daemon while listening; no Bonjour/Avahi runtime dependency. Real multicast smoke plus independent macOS Bonjour resolution verified both services, updates, IPv4/explicit-loopback address selection, withdrawal and re-enable (`docs/LOOP_LOG.md`, iteration 26). Still to do: Python/Blender wiring (1.2.5), iOS discovery (1.4.3), Windows/Linux runtime verification. |
| NET-002 | Partial | Rust `UdpReceiver` (task 1.2.1): one datagram per pose, never retransmitted; newest `seq` wins via `SeqFilter`; reordered or replayed poses counted as stale (`native/vcam-net/tests/udp_receiver.rs`, 6 loopback tests). Exposed to Blender as `vcam_native.Session.latest_pose()` (1.2.5a); pose apply is 1.3.1/1.3.2. The receiver waits with `mio` readiness polling (`native/vcam-net/src/udp.rs:350`, `:525`), not `SO_RCVTIMEO`: on Windows a timed-out blocking `recv` loses a datagram arriving at that moment (probe under CPU load: 15–21 of 960 lost with the timeout, 0 of 1,920 with `mio`), which flaked `control_server` in CI (2026-09-25). |
| NET-003 | Partial | Host side done (task 1.2.4). Authenticated CLOCK requests at 1 Hz on a monotonic host clock; a request is recorded only once sent (`native/vcam-net/src/udp.rs:262-273`; timing test `native/vcam-net/tests/udp_receiver.rs:209`). `vcam-protocol` `ClockEstimator` (`native/vcam-protocol/src/clock.rs:51-123`): replies must match one of the last 4 requests, are consumed once, must arrive < 2 s, and impossible timestamps are rejected. Offset/δ come from the lowest-δ sample of an 8-sample window (newest on ties); jitter is the integer RMS of θ. It matches `testdata/vcp/clock_sync.json` (`native/vcam-protocol/tests/golden.rs:276`; 4 mutations caught). Per-session estimate and rejected-reply count are in `ReceiverStats::clock`/`clock_rejected` (`udp.rs:221`, `:303`). The host clock is exposed as `host_clock_ns()` (`udp.rs:435`, `control.rs:247`) for `ClockEstimate::host_time_ns` (`clock.rs:44`). A real loopback test with a device clock 3.5 s ahead estimates the offset within 5 ms, rejects replayed/unrequested replies, and resets on a new session (`udp_receiver.rs:246`, 10/10 runs). Device replies implemented in 1.4.2c1 (`SightlineIOS/SightlineIOS/TrackingPipeline.swift:407-420`): authentic CLOCK requests echo t1 and stamp t2/t3 with CLOCK_UPTIME_RAW. Real UDP smoke authenticated the reply; simulator boundary coverage is in `SightlineIOSTests/TrackingPipelineTests.swift:463`. O-1 remains owner-blocked: uptime is the candidate ARFrame clock, not yet physically verified; capture-to-host latency accuracy is not established. |
| NET-004 | Partial | Host reconnect replaces the authenticated TCP/UDP session, closes old TCP, rejects old keys and resets samples/sequence filters (`native/vcam-net/tests/control_server.rs:372`); reconnect after the 10-second idle timeout is covered at `:210`. STATUS (2 Hz/on change) and CLOCK (1 Hz) now follow the latest authenticated UDP source; forged traffic cannot redirect them, and replacement/revocation clears routing and outbound state (`native/vcam-net/tests/udp_receiver.rs:313`, task 1.2.2b.2b). `.blend` reload (task 1.3.4): the session, its sockets and its poll timer survive `open_mainfile`/`read_homefile` (`tests/blender/addon_robust.py:117`); the file name is re-advertised (`core/session.py:228`). Device side (task 1.4.2b): the run stops when Blender closes the TCP connection or sends `ERROR` (`SightlineIOS/SightlineIOS/TrackingSessionController.swift:249`), and stopping closes it (`:261`; `SightlineIOSTests/VCPSessionClientTests.swift:161`). Device liveness done in 1.4.2c1: no authenticated CLOCK/STATUS for 3 s closes UDP and cancels controls without needing AR frames (`TrackingPipeline.swift:379-403`); the controller closes TCP and reports session lost (`TrackingSessionController.swift:300-308`). Session-tagged snapshots and run-tagged socket/timer callbacks reject old events. Tests cover heartbeat renewal, bad tags, stale acknowledgements, initial silence, stop/replacement and unpaired runs (`SightlineIOSTests/TrackingPipelineTests.swift:463-546`). Automatic reconnect done in 1.4.2c2: a lost session (TCP closed/`ERROR`, or 3 s of silence) no longer stops the run. AR keeps running, nothing is sent, and `VCPReconnect.run` (`SightlineIOS/SightlineIOS/VCP/VCPSessionClient.swift:381-430`) retries `HELLO(mode 1)` with the stored pairing. Attempts start ≤ 500 ms apart and are capped at 2 s each. Only a rejected pairing (`ERROR` 3/4, unknown host, bad proof) is fatal (`:380`). The new session re-binds the pipeline, so seq/state_seq restart and the full `CONTROL_STATE` goes out (`TrackingSessionController.swift:300-352`). An explicit stop cancels a pending start/reconnect and closes its TCP connection at once through the task cancellation handler (`VCPSessionClient.swift:361`, `TrackingSessionController.swift:354`). A run ended by backgrounding restarts on return to the foreground (`:380-400`). Tests: host down → back on the same port, session 301 ms after it listens, 4 attempts ≤ 500 ms apart (`SightlineIOSTests/VCPSessionClientTests.swift:340`); a rejected pairing stops after 1 attempt (`:382`); a cancel mid-handshake returns `.closed` in < 1 s and the host sees EOF (`:417`). Live Rust host restarted on the same port and pairing store (`:534`): new session 226 ms after the host was listening again, and its first CONTROL_STATE was acknowledged. Physical Wi-Fi roaming/sleep validation needs the owner. |
| NET-VID-001, NET-VID-004 | Partial | Task 2.2a (host encoder). `JpegWorker` (`native/vcam-video/src/jpeg.rs:259`, loop `:330`) takes the newest frame from the render `FrameSlot` (blocking `wait_take`/`wake`, `frame.rs:193`, `:203`), encodes it upright as 4:2:0 JPEG with `turbojpeg` 1.5.1 (`JpegEncoder::encode`, `jpeg.rs:110`; quality 1–100, default 80, adjustable while running) and publishes a newest-wins `EncodedFrame` carrying `frame_id`, `pose_seq`, `render_time_ns` and the sRGB/Rec.709 tag (`EncodedSlot`, `wait_take`/`wake` `:202`, `:212`). Unit tests `jpeg.rs:399-530` (3 mutations caught). Real Blender readbacks through the worker matched S-2a (Solid 540p q80: 52 KB, PSNR 39.2 dB, 0.89 ms). Task 2.2b (wire format): `VIDEO_FRAGMENT` (0x05) is specified in `docs/protocol/vcp.md:233` (32-byte header repeating `frame_id`, `render_time_ns`, `pose_seq`, codec, colour and quality in every fragment; 1148-byte data limit; newest-frame-wins reassembly). `native/vcam-protocol/src/video.rs`: `VideoFragment::decode`/`encode` (`:52`, validation `:128`), zero-copy host split `fragment_frame` (`:156`) and device `Reassembler::push` (`:247`: at most one frame in progress, stale/duplicate/inconsistent fragments dropped, lost frames counted, buffers reused). `Endpoint` seals/opens it host → device only (`endpoint.rs:118`, `:202`, `:217`); `Message` borrows fragment data (`Message<'a>`). Vectors `testdata/video/{fragments,reassembly}.json` pass in `vcam-protocol/tests/video.rs:79,138,185`; 5 mutations caught. Smoke: 9 real Blender readbacks (Solid/Material/EEVEE × 540p/720p/1080p) JPEG-encoded, fragmented, sent over loopback UDP reordered: intact frames reassembled byte-exact and decoded at the right size (Solid 540p: 52 KB, 46 fragments); frames with dropped datagrams were lost and superseded. Task 2.2c1 (sending): `VideoSender` (`native/vcam-net/src/udp.rs:599`, from `UdpReceiver::video_sender` `:527` / `ControlServer::video_sender` `control.rs:271`) numbers frames per session from 1 and validates them before using a number (`begin`, `udp.rs:695`), then seals each fragment with the session keys and sends it in index order to the latest authenticated source, taking the session lock per fragment (`step`, `:641`); a session change or stop ends the frame, a full socket is retried for at most 50 ms (`:39`), and `ReceiverStats` counts frames/fragments sent and frames failed. The sender holds only weak references; `stop` drops the socket under the state lock (`:545`), so the port is released. Tests `native/vcam-net/tests/udp_receiver.rs:563`, `:657` (4 mutations caught) and end to end over a real pairing with the fake iPhone, which now reassembles `VIDEO_FRAGMENT`s (`vcam-fake-iphone/src/main.rs:378`; `tests/fake_iphone.rs:281`: 20 frames, byte-exact). Smoke: 90 real Blender readbacks at 30 fps through `JpegWorker` → `VideoSender` → fake iPhone: 90/90 complete, 0 lost, 0 failed; send time p50 0.26 ms (540p, 37 fragments) to 0.87 ms (1080p Solid, 130 fragments), max 1.11 ms; the newest received JPEG was byte-identical and displayed upright. Still to do: `vcam_native`/add-on wiring, headless-Blender fake-iPhone test and libjpeg-turbo notices (2.2c2), adaptive quality (2.2d), Swift reassembly/display (2.3). Task 2.2c2a (Python boundary): `vcam_native.Session.start_video(slot, quality)` (`native/vcam-py/src/lib.rs:249`) runs `VideoPipeline` (`native/vcam-py/src/video.rs:81`): the `JpegWorker` plus a `vcam-video-send` thread (`:169`, `:179`) that hands each newest encoded frame to the session's `VideoSender` (JPEG codec, sRGB/Rec.709 colour, quality); frames with no device are counted as `unsent`. `stop_video`/`set_video_quality`/`video_stats` (`lib.rs:269-289`); `Session.stop` joins the video threads before closing the sockets (`:227`). Headless Blender `tests/blender/video_native.py` with the fake iPhone: 40 of 40 frames received, newest wire id/JPEG length/pose seq match, quality change reaches the stream, received JPEG decodes upright at 640×360 with the source colours (3 mutations caught). libjpeg-turbo IJG/BSD notices ship in `BlenderAddOn/THIRD_PARTY_NOTICES.md`. Task 2.2c2b (add-on): the stream loop starts `start_video` on a fresh `FrameSlot` when it creates its renderer (`BlenderAddOn/core/session.py:263`) and `stop_video` when it closes (`:128`: device session end, camera loss, undo, stream failure, `stop`), so no frame of one device session or camera reaches the next. The N-panel shows sent/skipped/quality, the last frame's size, JPEG KB, encode and send time, and failures (`core/status.py:61`, `ui/panels.py:93`; `BlenderAddOn/tests/test_status.py:40`). `tests/blender/render_session.py` passes rendered frames on to the encoder through a recording stand-in (`:40`) and checks that 960×540 and then 1080p frames reach the fake iPhone's session and that the video stream stops with the stream loop and restarts with it (`:145`; 2 mutations caught). Adaptive quality (NET-VID-005, 2.2d) and device playback (2.3) remain. |
| NET-VID-002/003 | Not started | S-2b/c (SRS §13.2), macOS: VideoToolbox H.264 720p is 3.6/5.6 ms (med/p95); no software H.264 is shipped (owner decision). Stage B is Phase 3. |
| NET-VID-005 | Partial | Task 2.2d1 (feedback wire format). `VIDEO_REPORT` (0x07, device → host, 16 bytes) is specified in `docs/protocol/vcp.md:281`: `report_seq`, `newest_frame_id`, `frames_complete` (session totals, so a lost report loses nothing) and `m2p_p95_ms` (0 = not measured), sent every 500 ms once frames arrive; the host derives loss, including frames none of whose fragments arrived, from `newest_frame_id`. Vectors `testdata/video/report.json` (8 cases, generator self-checks the §6.6 example). Rust codec `VideoReport` (`native/vcam-protocol/src/video.rs:182`; rejects `frames_complete` > `newest_frame_id`), device-only direction (`endpoint.rs:205`), `Reassembler::report` (`video.rs:302`); golden test `native/vcam-protocol/tests/video.rs:236`. The host receiver keeps the newest report per session in `ReceiverStats::video_report` (`native/vcam-net/src/udp.rs:267`; test `native/vcam-net/tests/udp_receiver.rs:154`). The fake iPhone sends reports (`native/vcam-fake-iphone/src/main.rs:342`), checked end to end at `tests/fake_iphone.rs:342`. 6 mutations caught. Task 2.2d2a (host controller, pure Rust): `VideoAdapter` (`native/vcam-net/src/adapt.rs:99`) counts each report interval's loss per §6.6 from the frames the sender finished (`frame_sent`) and the report (`count`, `:249`), as a session total, so a frame still arriving at one report is lost once and taken back later. Policy (`report`, `:184`; constants `:17-36`): more than 10 % of ≥ 5 frames lost, or M2P p95 above 120 ms (NFR-LAT-003), in 2 consecutive reports lowers quality by 10 down to 50, then resolution by one step up to a caller-given limit (`lower`, `:273`); 10 consecutive clean reports (≤ 2 % loss, M2P ≤ 96 ms or unmeasured) raise resolution first, then quality back to the user's (`raise`, `:294`); 2 reports settle after each change; every change is kept with its reason for the UIs (`AdaptChange`, `AdaptStats`). 11 unit tests (`adapt.rs:373-583`), 16 mutations caught. Task 2.2d2b (in the pipeline): the `vcam-py` sender thread keeps one `VideoAdapter` per device session (`Adaptation`, `native/vcam-py/src/video.rs:83`; loop `run`, `:305`), feeds it every finished frame and the device's newest report (read with its session under one lock, `VideoSender::video_report`, `native/vcam-net/src/udp.rs:690`) and sets the encoder to the level's quality; a new or ended session starts again at the user's quality. A user quality change mid-session is followed unless the stream was lowered (`VideoAdapter::set_quality`, `adapt.rs:184`). `Session.start_video(slot, quality, max_resolution_drop=0)` and `video_stats()` (`user_quality`, `adapt`: level, loss totals, newest report, last interval, last change; `native/vcam-py/src/lib.rs:173`, `:315`). Unit tests `adapt.rs:606`, `video.rs:460`; end to end in headless Blender with the fake iPhone reporting M2P 150 ms (`--m2p`, `native/vcam-fake-iphone/src/main.rs:132`): quality 80 → 70 → 60 at reports 2 and 6, frames sent at 60, back to the user's quality once the device leaves (`tests/blender/video_native.py:125-170`). 8 mutations caught. Not yet: the add-on applying the resolution step and the N-panel (2.2d2c); the iPhone sending reports and showing the quality (2.3); M2P measurement (2.6). |
| LNS-* | Not started | |
| NFR-LAT-001 | Partial | Task 1.5.1a (host side). Each newly applied pose records the pose leg (host clock right after the apply − capture time mapped through the `CLOCK` offset) and the main-thread apply cost (`BlenderAddOn/core/session.py:252`, `:257`) into a per-device-session `LatencyLog` (`core/latency.py:78`; re-applies of the same seq aren't samples; seq gaps counted as not applied). `report()` (`:95`) gives p50/p95/p99, 0.5 ms histograms and the verdicts p95 ≤ 20 ms and every apply ≤ 1 ms; the `vcam.latency_report_save` operator writes it as `latency-<date>.json` (`operators/session.py:140`). Tests: `BlenderAddOn/tests/test_latency.py`, `tests/blender/pose_leg_latency.py` (fake iPhone, 60 Hz, in CI `blender-smoke`). Loopback on macOS: `reports/latency-2026-09-25-macos-arm64-loopback.json`, pose leg p95 16.8 ms, and SRS §13.4 flags that the 60 Hz poll alone costs 0–16.7 ms. Still open: the Wi-Fi/wired measurement with a real iPhone (owner; needs 1.4.2b and O-1). |
| NFR-LAT-002 | Partial | Task 1.5.1b (simulator; device run owner-blocked). Send leg = frame reaching `TrackingPipeline.receive` (`SightlineIOS/SightlineIOS/TrackingPipeline.swift:253`) → POSE handed to the kernel; recorded per run in `SendLegMeter` (`:116`, 10 µs bins, allocation-free) and shown as "Send leg p95" in Settings (`SettingsView.swift:65`). No allocation per pose: seal into a reused buffer (`VCP/VCPEndpoint.swift:56`) with CommonCrypto HMAC (`:114`), BSD UDP socket `send(2)` (`UDPSender.swift:128`), closure-free actor entry (`TrackingPipeline.swift:258`). Tests: `SightlineIOSTests/TrackingPipelineTests.swift:296` (0 heap blocks over 600 poses, Release; CI step `.github/workflows/ci.yml:253`) and `:313` (p95 ≤ 2 ms). Simulator p95 0.02 ms (SRS §13.5). |
| NFR-LAT-003/004, NFR-PERF-001 | Not started | S-1 measured render/readback (§13.1); the 1.5.1 report uses the NFR-LAT-004 JSON format. M2P legs and device thermal testing await video and hardware. |
| NFR-PERF-002 | Partial | Default Solid 960×540/30 fps uses a saved 12 ms target (`BlenderAddOn/core/render.py:28-55,160-170`). A 7 s GUI run in iteration 61 delivered 172 frames with timer p95 26.3 ms, max measured read 3.0 ms and draw 1.1 ms; `tests/blender/render_session.py` verifies mode changes, not sustained performance. A longer GUI soak, Windows/Linux GPU checks and the 50 ms worst-case bound remain unverified: skipping cannot interrupt one blocking GPU call. EEVEE is exempt. |
| NFR-REL-001 | Partial | Rust parsers never panic on network data (clippy-enforced no `unwrap`; prefix/flip/random sweeps; fuzz targets). The receiver thread survives bad datagrams and socket errors. PyO3 boundary: every `Session` method runs under `guard` (`native/vcam-py/src/lib.rs:23`), which turns a panic into `vcam_native.NativeError`, a `RuntimeError` subclass (`:15`) that `except Exception` catches, instead of PyO3's `PanicException` (a `BaseException`). Verified in Blender through `_panic_probe` (`:73`; `tests/blender/session_native.py`), and the session survives it; removing the guard fails the test. Caller mistakes raise `ValueError` (`io_err`, `:88`). Still to do: add-on-level status messages (1.3.x). |
| NFR-REL-002 | Partial | `ControlServer::stop()` joins TCP/UDP workers, clears keys/samples and withdraws owned DNS-SD (`native/vcam-net/src/control.rs:288`, `discovery.rs:49`). DNS-SD shutdown acknowledgement is bounded to 500 ms and errors are returned; stopped servers cannot advertise (`native/vcam-net/tests/control_server.rs:496`). Real multicast smoke measured 38 ms stop and verified withdrawal and re-enable (iteration 26). From Python: `Session.stop()` (`native/vcam-py/src/lib.rs:155`) is idempotent, releases the GIL, and took 50 ms in headless Blender. The TCP port then refuses connections and a new `Session` binds the same TCP and UDP ports (`tests/blender/session_native.py`). The add-on's `unregister()` stops the session and its timer poll first (`BlenderAddOn/__init__.py:24`, `core/session.py:266`). Disabling the add-on with a session running took 84 ms; the port then refuses connections, and re-enabling starts on the same port with the same host ID (`tests/blender/addon_session.py`, task 1.2.5b; 3 mutations caught). |
| NFR-REL-003 | Not started | |
| NFR-SEC-001 | Partial | Rust: SRP-6a, HKDF session keys, constant-time proofs, authenticated UDP, single-use/expiring codes and failed-proof lockout. Pairing keys persist atomically before PAIR_ACCEPT (`native/vcam-net/src/store.rs:37-126`, `control.rs:468-483`; storage-failure/private-file tests `native/vcam-net/tests/control_server.rs:632`, `:766`). Verified TCP sessions own UDP keys, revoked on replacement/disconnect/10-second idle expiry/shutdown (`udp.rs:166`, `:330`, `:354`, `:429`). Outbound STATUS/CLOCK uses those keys and only an authenticated source; stale-session status updates are refused (`udp.rs:218`, `:375`; tests `native/vcam-net/tests/udp_receiver.rs:246`, `:313`, 1.2.2b.2b). Blender keeps the pairings and a stable host ID in the extension's user directory (`BlenderAddOn/core/session.py:59`, `:85`, task 1.2.5b); the ID file is mode 0600, and a damaged file is rejected, not replaced. iOS device side of pairing and session setup (task 1.1.4b): SRP-6a per vcp.md §9.2 (RFC 5054 App. B and `pairing.json` byte-exact, `SightlineIOSTests/VCPGoldenTests.swift:253`, `:264`), abort on `B mod N = 0` and non-digit codes (`:295`), constant-time M2 and `proof_h` checks through CryptoKit `HMAC.isValidAuthenticationCode` (`SightlineIOS/SightlineIOS/VCP/VCPPairing.swift:95`), session keys that seal `session.json`'s first POSE byte-exact (`:317`). Over TCP (task 1.4.2b) the device sends no `SESSION_PROOF` to a host whose `host_id` it has no key for and nothing after a `proof_h` that fails (`SightlineIOSTests/VCPSessionClientTests.swift:205`, `:180`). Note: `swift-srp`'s BoringSSL `BN_mod_exp` isn't constant-time in the exponent (a fresh `a` per attempt). Still to do: iOS Keychain (1.4.3b). |
| NFR-SEC-002 | Not started | |
| NFR-SEC-003 | Partial | Host side: Blender listens only while a session is running. Enabling the add-on opens no ports; `vcam.session_start`/`vcam.session_stop` (`BlenderAddOn/operators/session.py`) and disabling the add-on control both ports (`tests/blender/addon_session.py`, task 1.2.5b). No analytics code exists. iOS: the camera usage string (1.4.1) and the local-network usage string plus the single browsed Bonjour type `_vcam-ctl._tcp` (`SightlineIOS/Info.plist`, task 1.4.3a). |
| NFR-QA-001 | Partial | One git repo. `xcodebuild test`, `cargo test` (in `native/`), and headless Blender tests of the installed extension (`tests/blender/smoke_native.py`, `session_native.py`, `addon_session.py`, `addon_apply.py`, `addon_panel.py`, `addon_robust.py`, `pose_leg_latency.py`) all run. All seven are in the CI `blender-smoke` job on 3 OSes; the four that need the fake iPhone use a release build made in that job (task 1.3.5). All passed on Linux, Windows and macOS in PR #2 run `36096344645`. There's no single `blender --background … tests` runner for add-on logic yet. |
| NFR-QA-002 | Done | `ci.yml` runs on every PR: fmt, clippy (`-D warnings`) and test on 3 OSes (NASM installed on Linux/Windows for turbojpeg-sys, `ci.yml:49-57`), a time-boxed `fuzz` job (nightly + `cargo-fuzz 0.13.2`, 60 s per target, seeded from `testdata/`, artifacts uploaded on failure), Python tests, headless Blender on 3 OSes (including the fake-iPhone integration tests, task 1.3.5), and iOS unit tests (result bundles uploaded on failure, `ci.yml:275-280`). `ci-ok` gates merges to the protected `main`. First fully green run: PR #2 run `36096344645`, 15 of 15 jobs. |
| NFR-QA-003 | Partial | `testdata/` holds VCP message, receive-rule, pairing/session, clock-sync (`vcp/clock_sync.json`, task 1.2.4), coordinate (task 1.1.2) scripted-motion (`motion/scripted.{json,bin}`, task 1.2.7: 390 frames at 60 Hz, 5 keyposes with `matrix_world`, self-checked look directions) and rig vectors (`rig/rig_cases.json`, tasks 1.3.2a/b: 11 cases, including the expected tilt/dolly/crane keyposes of the scripted-control fake-iPhone run, computed with rotation matrices independently of the quaternion implementation). Consumed by Rust (`vcam-protocol/tests/`) and Swift (`SightlineIOSTests/VCPGoldenTests.swift`, bundled via a `testdata` folder reference). Clock sync is host-only, so Swift doesn't consume `clock_sync.json`. Hold vectors `rig/hold.json` (task 1.3.6: 5 pose/tracking-state/option sequences and the scripted limited span). Python consumes `rig_cases.json`, `hold.json` and `freshness.json` (`BlenderAddOn/tests/test_rig.py`); fragment vectors `video/fragments.json` (16 VIDEO_FRAGMENT datagrams: 4 accepted, 12 rejected with reasons) and reassembly vectors `video/reassembly.json` (6 sequences) are consumed by Rust (`vcam-protocol/tests/video.rs`); Swift consumption follows in 2.3. CI checks the vectors are up to date. |
| NFR-QA-004 | Partial | Enforced by workspace lints: `unsafe_code` deny plus `clippy::undocumented_unsafe_blocks` deny (`native/Cargo.toml:22-32`). Checked with a throwaway probe: clippy rejected an uncommented `unsafe` block. |

---

## Next milestone

**Phase 0 exit:** single repo; `DesktopReceiver/` moved to `legacy/`; Rust workspace with a PyO3 module importing inside Blender 5.2 on all three OSes via CI; spikes S-1 (render/readback), S-2 (encoders), and S-3 (macOS loading) answered.
