# Implementation Progress

| Field | Value |
|---|---|
| Assessed | 2026-09-24 |
| Against | `docs/SRS.md` v3.0 (requirement IDs below refer to it) |
| Plan | `docs/IMPLEMENTATION_PLAN.md` v3.0 |

Status labels: **Done** · **Partial** (useful code exists but doesn't meet the requirement yet) · **Replace** (works, but the v3 architecture replaces it) · **Retire** (no longer in scope) · **Not started**.

## Summary

| Area | Status | Reality |
|---|---|---|
| iOS tracking (`VCamIOS/`) | Partial | Headless ARKit session sending FreeD over UDP. Good base for FR-TRK-001/005. The packet format and Euler conversion must be replaced with VCP + quaternions. |
| Blender extension (`BlenderAddOn/`) | Partial | Uses `vcam_native` only (task 1.3.1): a session starts on request, and a 60 Hz main-thread poll applies the newest canonical pose to the target camera and publishes STATUS. The Python FreeD path is deleted. No `VCam_Origin` rig or full N-panel yet (1.3.2/1.3.3). Manifest targets 5.2 and bundles the `vcam_native` wheel (macOS arm64 only so far). |
| Rust native module | Partial | `native/` workspace with 5 crates. `vcam-py` builds the PyO3 module `vcam_native` (only `version()` so far), which imports inside Blender 5.2.2 on macOS (task 0.1.4). |
| Viewfinder stream (Blender → iPhone) | Not started | — |
| C++ `legacy/DesktopReceiver/` (incl. CMIO extension) | Retire | Moved to `legacy/` in task 0.1.2 (ARC-006). Port the parsers, their tests, and the test-pattern generator to Rust, then delete the directory. |
| Repo / CI | Partial | One git repo, private remote `origin`. CI run `35958600428` (GitHub, 2026-09-24): 10 of 13 jobs green (wheels ×3, split extension, Blender smoke ×3, rust-fmt, pytest, iOS). `cargo clippy` failed on all 3 OSes because of a new Rust 1.98 lint; fixed locally in loop iteration 15, and it needs a push to confirm. |

**Maturity:** early prototype. Phase 0 tasks 0.1.1–0.1.6 are done. The extension with the Rust module installs and imports on all 3 OSes in CI. CI is not fully green yet (clippy fix awaiting push). Spikes S-1, S-2, S-3 have macOS records; Windows/Linux parts are owner-blocked. Phase 1 started (1.1.1 done).

**Environment on the owner's Mac (2026-09-24):** Blender 5.2.2 LTS at `/Applications/Blender.app`; cargo/rustc 1.97.1; `.venv.nosync/` with pytest + maturin; Xcode 27.0 (via `DEVELOPER_DIR`) with the iOS 27.0 simulator.

## Last verified test runs (2026-09-24)

| Suite | Result | Note |
|---|---|---|
| Legacy C++ (`legacy/DesktopReceiver`) | 9/9 pass (fresh temp build dir) | Re-run after the move to `legacy/`. To be retired. The stale in-repo `build/` was deleted. |
| `BlenderAddOn/tests` (pytest, `.venv.nosync`) | 19/19 pass | Host-ID persistence (`test_session_host_id.py`); N-panel formatting (`test_status.py`, task 1.3.3); rig math against `testdata/rig/rig_cases.json` and control merging against `freshness.json` (`test_rig.py`, task 1.3.2a). The FreeD parser/transform tests were deleted with the FreeD path (task 1.3.1). Add-on behaviour is tested in headless Blender (`tests/blender/`). |
| `native/` cargo fmt/clippy/test | Pass, 5/5 crates | Includes `vcam-py` with pyo3 0.29.2. |
| Headless Blender `tests/blender/smoke_native.py` (macOS arm64) | Pass | Built extension zip, installed into a temporary user dir, enabled; printed `VCAM_NATIVE_OK 0.1.0`. |
| S-1 `tests/bench_render.py` (macOS arm64) | Pass, 9/9 cells in each of 3 runs | Runs: headless as fast as possible (S-1a); headless at 30 fps and in the GUI with timers (S-1b). Numbers in SRS §13.1, JSON in `reports/s1-render-2026-09-24-macos-arm64*.json`. |
| S-2a `vcam-video` example `s2_jpeg` (macOS arm64) | Pass, 6 frames × 3 qualities × 2 encoders | Every JPEG decoded at the right size; PSNR 37.9–47.6 dB. Numbers in SRS §13.2 and `reports/s2-jpeg-2026-09-24-macos-arm64.txt`. |
| S-2b `vcam-video` example `s2_videotoolbox` (macOS arm64) | Pass, 3 res × 2 configs (RGBA input unsupported, reported) | 160/160 frames, 0 dropped; ffmpeg decoded all 3 streams without errors (Main, no B-frames). Numbers in SRS §13.2 and `reports/s2-videotoolbox-2026-09-24-macos-arm64.txt`. |
| S-2c `vcam-video` example `s2_openh264` (macOS arm64) | Pass, 3 res × 2 builds × 2 thread counts | 160 frames each; ffmpeg decoded all 6 streams without errors (Constrained Baseline, no B-frames). Numbers in SRS §13.2 and `reports/s2-openh264-2026-09-24-macos-arm64.txt`. |
| VCamIOS `xcodebuild test` (iPhone 17 Pro sim, iOS 27.0) | 17 pass, 1 opt-in skip (2026-09-25) | 6 VCP golden, 6 tracking pipeline (tasks 1.4.1/1.4.2a), 6 host discovery (task 1.4.3a; the Rust-advertiser interop test is opt-in via `TEST_RUNNER_VCAM_INTEROP_HOST` and passed when run against a live `vcam_native` host). After the checksum overflow fix (root commit `5ba2672`, was `ed349be` in the old `VCamIOS` repo). |

---

## By SRS section

### §3 Architecture

| ID | Status | Evidence / gap |
|---|---|---|
| ARC-001 | Done | Extension + PyO3 crate `native/vcam-py` (`src/lib.rs`, maturin config in `pyproject.toml`), bundled as per-platform wheels. CI run `35958600428` built cp313 wheels for manylinux_2_28 x86-64, win_amd64, and macOS arm64, assembled them with `extension build --split-platforms`, and each platform zip imported `vcam_native` in Blender 5.2.2 (`blender-smoke` ×3). |
| ARC-002 | Partial | Crates split as required (`native/Cargo.toml:3-9`). `vcam-protocol` is `#![forbid(unsafe_code)]` (`native/vcam-protocol/src/lib.rs:5`), with no I/O. It now has the UDP framing/messages (task 1.1.3a). No net or production encoder code yet. |
| ARC-003 | Partial | Blender consumes only the canonical pose (vcp.md §6.1/§7): `BlenderAddOn/core/apply.py:34` builds the matrix from position + quaternion with no axis conversion, because ARKit→canonical happens once on the device (DM-002). Verified to within 6.3e-8 of the independently generated `testdata/motion/scripted.json` keyposes (`tests/blender/addon_apply.py`, task 1.3.1). iOS still sends FreeD Euler (1.4.2). |
| ARC-004 | Not started | iOS doesn't send a capture time or sequence number; Blender stamps receive time. |
| ARC-005 | Partial | Task 1.4.1. Swift 6 language mode for the app and test targets (`VCamIOS/VCamIOS.xcodeproj/project.pbxproj:250,269,423,456`), building with no errors or warnings. ARKit callbacks and UDP I/O are off the main actor: the `TrackingPipeline` actor runs on its own `DispatchSerialQueue` (`VCamIOS/VCamIOS/TrackingPipeline.swift:46`), which is also `ARSession.delegateQueue` (`TrackingSessionController.swift:38`) and the `NWConnection` queue, so frames and send completions enter the actor with no hop. The main actor gets snapshots at ≤ 15 Hz (`UIThrottle`, `TrackingPipeline.swift:17`) through a newest-wins `AsyncStream`. The `@Observable` controller replaces `ObservableObject`. Tests: `VCamIOSTests/TrackingPipelineTests.swift` (60 Hz → exactly 15 Hz, off the main thread; ±4 ms jitter never over 15 per second; frames dropped after stop). Mutations caught: a mutable property on the delegate failed the Swift 6 build; removing the throttle and ignoring stop failed the tests. Not verified: the ARKit delegate-queue path on a device (the simulator has no ARKit); video decode off the main actor is Phase 3. |
| ARC-006 | Partial | Moved to `legacy/DesktopReceiver/`, with a retirement note at `legacy/DesktopReceiver/README.md:1-6`. Porting to Rust and deleting the directory are still open. |
| ARC-007 | Not started | Optional. |

### §4 iOS app

| ID | Status | Evidence / gap |
|---|---|---|
| FR-TRK-001 | Partial | Task 1.4.2a. One VCP `POSE` per `ARFrame` (`worldAlignment = .gravity`): `seq` from 1 per run, `capture_time_ns` = `ARFrame.timestamp` in ns, canonical position + quaternion via `VCPCoordinates` (`VCamIOS/VCamIOS/TrackingPipeline.swift:106-130`). Sealed with the session's device key and sent over UDP when a session exists. The first frame of vcp.md §6.1's example arrives on a loopback socket as `pose_normal.bin` (authenticated by the host key; header, seq, time, position and state exact; the quaternion within 1 ulp) (`VCamIOSTests/TrackingPipelineTests.swift:152`). Missing: the session itself (pairing 1.1.4b, owner-blocked, then 1.4.3), so the app doesn't send yet; a device run; O-1 (which clock `ARFrame.timestamp` uses; the SDK header doesn't say, so it needs a device check). |
| FR-TRK-002 | Partial | The device side is done (task 1.4.2a): every `POSE` carries the vcp.md §6.1 `tracking_state` code taken from its own `ARFrame` (`VCamIOS/VCamIOS/VCP/VCPTrackingState.swift:13`, table test in `TrackingPipelineTests`), and the UI shows the state. Still missing: the Blender "hold last good pose while limited" option. |
| FR-TRK-003 | Partial | Host side of Set origin done (task 1.3.2a): any change of `origin_epoch` after the first one seen in a session (the shared `freshness.json` rule) re-zeros position and heading to the current pose (`BlenderAddOn/core/rig.py:62`, `:70`). The zero is stored on `VCam_Origin` (`core/apply.py:110`, `:124`), so it survives reconnects and saves. Verified over the real wire with the fake iPhone's `--set-origin-at` (task 1.3.2b). The iOS button is 1.4.4. |
| FR-TRK-004 | Not started | |
| FR-TRK-005 | Done | `handleScenePhase` stops tracking on inactive/background. |
| FR-TRK-006/007 | Not started | |
| FR-TRK-008 | Done | Headless `ARSession`; no camera preview. |
| FR-VF-* | Not started | |
| FR-CTL-004 | Partial | Host side done (task 1.3.2a). `CONTROL_STATE` motion scale and locks (height, roll, pan-only) are merged per vcp.md §6.2 (`BlenderAddOn/core/rig.py:124`; absent fields kept, stale `state_seq` ignored) and applied in the rig step (`rig.py:109`), with `control_ack` echoed in STATUS (`core/apply.py:150`). Checked against `testdata/rig/rig_cases.json` (pytest) and through real Blender objects (`tests/blender/addon_apply.py`). Over the real wire (task 1.3.2b), `vcam-fake-iphone --scale 2 --locks 1 --set-origin-at 105` into the add-on in headless Blender matches the `scripted_*` rig vectors to within 6e-8, with `control_ack=2` seen by the device (`tests/blender/addon_apply.py`). Still to do: the iOS controls UI (1.4.4). |
| FR-CTL-001..003, 005..009 | Not started | |
| FR-UX-001/002 | Partial | iOS discovery done (task 1.4.3a): `HostBrowser` runs `NetworkBrowser` over `_vcam-ctl._tcp` with TXT (`VCamIOS/VCamIOS/HostDiscovery.swift:72`, `:78`), and each host is listed by `host=` machine and `blend=` file ("Unsaved file" when empty), deduplicated per interface and sorted (`:44`). The list is the first section of the UI, browsing while it's visible (`ContentView.swift:17`, `:78`); manual host entry remains. Verified against an `NWListener` advertiser (listed, TXT update, withdrawal; `VCamIOS/VCamIOSTests/HostDiscoveryTests.swift:91`) and the real Rust `mdns-sd` advertiser (`:123`). `NSLocalNetworkUsageDescription` and `NSBonjourServices` are in `VCamIOS/Info.plist` (C-4). Rust `ControlServer` handles code-based SRP pairing (1.2.2a). Pairings survive host restart via `FileStore` (`native/vcam-net/src/store.rs:37`, 1.2.2b.1), verified over TCP without another code (`native/vcam-net/tests/control_server.rs:607`). Host DNS-SD is available through `ControlServer::advertise` (`native/vcam-net/src/control.rs:203`, 1.2.3). The add-on keeps a stable host ID and the pairing store in the extension's user directory (`BlenderAddOn/core/session.py:59`, `:85`, task 1.2.5b). The N-panel shows the pairing code (task 1.3.3). Still to do: selecting a listed host, connecting, pairing UI and Keychain (1.4.3b, needs Swift pairing 1.1.4b, waiting for the owner's `swift-srp` decision). |
| FR-UX-003/004 | Not started | Portrait `Form` UI (`ContentView.swift`). |
| Pose maths | Replace | `TrackingPose.swift` uses an aerospace Euler extraction on ARKit's Y-up frame. The replacement exists: `VCamIOS/VCamIOS/VCP/VCPCoordinates.swift` (DM-002 quaternion conversion, task 1.1.4a). Wiring it into the tracking path is task 1.4.2. |
| Project settings | Partial | Deployment target iOS 26.4; `SWIFT_VERSION = 5.0`; the camera usage string contains zero-width spaces (U+200B). |

### §5 Blender extension

| ID | Status | Evidence / gap |
|---|---|---|
| FR-BL-001 | Done | `blender_version_min = "5.2.0"`, `network` permission, `wheels`/`platforms`. CI writes all three platforms into the manifest (`tools/set_manifest_wheels.py`) and produced `windows-x64`, `linux-x64`, and `macos-arm64` zips that install and import (run `35958600428`). The committed manifest lists macOS only, for local builds. `windows-arm64` (SHOULD) not built. |
| FR-BL-002 | Partial | The native module owns the sockets and threads (`vcam-net` `ControlServer`/`UdpReceiver`, tasks 1.2.1–1.2.4). They are now exposed to Python as `vcam_native.Session` (`native/vcam-py/src/lib.rs:82`, task 1.2.5a): `start`/`stop` release the GIL; `latest_pose`, `stats`, `poll_event` and `pairing_code` are non-blocking reads of latest-sample slots and the event queue (C-2). Verified in headless Blender 5.2.2 (`tests/blender/session_native.py`). The add-on starts it on request and stops it in `unregister()` (`BlenderAddOn/core/session.py:110`, `:130`, `:229`; `tests/blender/addon_session.py`, task 1.2.5b). The add-on's only transport is `vcam_native` (task 1.3.1): `core/session.py:215` applies each new pose via `core/apply.py:150`; the Python FreeD client is deleted. |
| FR-BL-003 | Partial | Rig done on the host (task 1.3.2a). `VCam_Origin` (reused if the user placed it, otherwise created at the camera's position) → the target camera, parented with an identity inverse (`BlenderAddOn/core/apply.py:90`). The camera's `matrix_basis` is the local pose: the device pose relative to the Set-origin zero, locks, then motion scale (`core/rig.py:109`). The incoming pose is never edited. Verified in headless Blender with the fake iPhone under a translated/rotated/scaled user rig: local error 1.8e-7, and world = origin × local (`tests/blender/addon_apply.py`). Scripted controls over the wire are verified too (task 1.3.2b). Still to do: the N-panel rig controls (1.3.3), and robustness to deleted/renamed objects (1.3.4). |
| FR-BL-004 | Done | N-panel (`BlenderAddOn/ui/panels.py`, task 1.3.3), with every item FR-BL-004 lists:
  - session start/stop;
  - pairing code, grouped "045 479", with Pair/Cancel (`operators/session.py`);
  - connected device, tracking state (vcp.md §6.1 names), pose rate, loss;
  - pose-leg latency: host clock − (capture − clock offset), computed at apply (`core/status.py`, `core/session.py:215`), plus clock jitter;
  - camera picker, smoothing toggle;
  - rig: motion scale "1:10" and locks from the device, shown read-only because the device owns CONTROL_STATE (FR-CTL-009), with host Set/Clear Origin.

  It redraws at 4 Hz from the session poll. Behaviour is tested in headless Blender with the fake iPhone (`tests/blender/addon_panel.py`; 3 mutations caught) and in pytest (`test_status.py`). The drawing was checked visually in a GUI Blender run (iteration 35: pairing and streaming screenshots). |
| FR-BL-005 | Not started | The FreeD zoom/focus mapping was deleted with the FreeD path (task 1.3.1). Lens controls arrive as T2 `CONTROL_STATE` fields (O-4). |
| FR-BL-006 | Partial | Native side done (task 1.2.6). The One-Euro filter (`native/vcam-net/src/smooth.rs:93-139`) runs position per axis and orientation by hemisphere-corrected slerp, with speed-adaptive cutoffs. dt comes from the device `capture_time_ns`, so network jitter doesn't change the filtering. A gap over 0.5 s, a non-increasing capture time, or a tracking-state change restarts the filter at the raw sample. Per-receiver toggle: `UdpReceiver`/`ControlServer::set_smoothing` (`udp.rs:453`, `control.rs:252`). The setting survives session changes, and the filter state resets per session and on every change. Raw is always kept: `PoseSample::pose` is raw and `::smoothed` is applied. Python: `Session.set_smoothing`/`smoothing()` and `latest_pose()["smoothed_position"/"smoothed_orientation"]` (`native/vcam-py/src/lib.rs:268`, `:320`). Tests: 8 filter unit tests (formula check, jitter < 10 % variance, adaptive lag for position and rotation, short-way quaternion, restarts, validation), a loopback test (`native/vcam-net/tests/udp_receiver.rs:169`), and Blender `tests/blender/session_native.py`; 9 mutations caught. Default parameters aren't tuned on a device yet. The add-on applies the smoothed pose (1.3.1/1.3.2), and the N-panel has a Smoothing toggle that applies to the running session and to later sessions (scene property, task 1.3.3; `tests/blender/addon_panel.py`). |
| FR-BL-007 | Done | Task 1.3.4. Nothing in the add-on keeps `bpy` data across ticks (reload and undo free it, `ReferenceError` otherwise). Persistent `load_post`/`undo_post`/`redo_post` handlers (`BlenderAddOn/core/session.py:228-263`) re-apply the live pose after undo or reload; on reload the running session and its timer continue, and the new file's smoothing and name are taken. The camera choice is checked every tick (`core/apply.py:53`): a camera deleted in the UI (it lingers outside the scene while the target pointer uses it, and `scene.camera` still names it) is not driven and not replaced by another camera; the device gets STATUS error 1 (no camera) and the N-panel shows a warning (`ui/panels.py:41`). A renamed camera keeps being driven and STATUS carries the new name; a renamed rig is still found through its `vcam_origin` marker (`core/apply.py:77`). Tested in headless Blender with the fake iPhone (`tests/blender/addon_robust.py`: camera and rig rename, undo, save + reopen, UI delete, replacement camera, empty file; the poll must record no error); 6 mutations, 5 caught directly and the 6th (rig found by name only) caught once its fallback was also removed. GUI screenshot (iteration 36): the warning `"Camera" was deleted` under the camera picker. |
| FR-REN-* | Not started | S-1 (SRS §13.1) shows headless `GPUOffScreen` + `draw_view3d` + `read()` works, and zero-copy access through `vcam_native._frame_probe` works (`native/vcam-py/src/lib.rs:18-32`). Production code comes in Phase 2. **Flag:** EEVEE draw (76 ms at 540p) conflicts with FR-REN-004/NFR-PERF-002. |
| FR-TAKE-* | Not started | |
| UI text | Done | "Live Link" removed (task 0.1.6). The FreeD operators were deleted in 1.3.1; the add-on now offers "Start VCam Session" / "Stop VCam Session" (`BlenderAddOn/operators/session.py`). |

### §6 Protocol

| ID | Status | Evidence / gap |
|---|---|---|
| DM-001..003 | Partial | Specified (`vcp.md` §6.1, §7). Rust pose type (`vcam-protocol`); Swift pose type and ARKit→canonical conversion (`VCamIOS/VCamIOS/VCP/VCPMessages.swift`, `VCPCoordinates.swift`, task 1.1.4a), no Euler angles. Not yet used by the live tracking path (1.4.2). Open items O-1/O-2 (device checks). |
| DM-004 | Partial | `testdata/coords/arkit_to_canonical.json` (9 cases) is consumed by Swift `VCPGoldenTests.testARKitToCanonicalMatchesDM004Vectors` (position, quaternion, `matrix_world` columns, view direction; task 1.1.4a). The Rust side has no ARKit conversion by design (DM-002); the Blender apply step (1.3.2) will use the canonical columns. |
| PR-004 | Done | Spec with byte layouts and example hex (`docs/protocol/vcp.md`) plus golden vectors in `testdata/vcp/`: `messages.json` + 10 `.bin`, `receive.json` (26 receive-rule cases), `freshness.json`, `pairing.json`, `session.json`, `srp-rfc5054-appendix-b.json`. Generated by `tools/gen_testdata.py`, which self-checks against RFC 5054 App. B, RFC 5869 A.1, and every `vcp.md` example. |
| PR-001..003, PR-006 | Partial | Rust (`native/vcam-protocol`): UDP framing, HMAC trailer, messages, TCP control frames, SRP-6a pairing, session keys (1.1.3a/b). Swift (`VCamIOS/VCamIOS/VCP/VCPEndpoint.swift`, `VCPMessages.swift`, 1.1.4a): UDP framing, CryptoKit HMAC-SHA256/64 with constant-time compare, receive rules, POSE/CONTROL_STATE/CLOCK/STATUS; byte-exact golden vectors. Host networking authenticates incoming pose/control and sends session-scoped STATUS/CLOCK (`native/vcam-net/src/udp.rs:218`, 1.2.2b.2b); wire tests `native/vcam-net/tests/udp_receiver.rs:209`, `:246`, `:313`. Applied acknowledgements are published through `ControlServer::update_status` (`control.rs:246`), never inferred from reception. Still to do: Swift TCP/pairing (1.1.4b), device sockets (1.4.x), Blender bindings (1.2.5). |
| PR-005 | Partial | `Endpoint::open` applies vcp.md §4.3 steps 1–8 in order with typed `DropReason`s. Decoding is bounds-checked (`src/wire.rs`), with no `unwrap` (clippy-enforced). It matches all 26 `receive.json` verdicts. Fuzz targets `native/fuzz/fuzz_targets/{udp_open,control_decode,pairing_inputs}.rs` (task 1.1.3c) assert round-trip and no-forgery properties. So far they've only had a **coverage-blind** 60 s run on stable (274.5M / 279.9M / 45k execs, 0 crashes). A coverage-guided `cargo fuzz` run (nightly) is pending CI or the owner's OK for a local nightly toolchain. |
| PR-FD-001 | Replace | Blender side removed (task 1.3.1: `core/freed_parser.py`, `udp_client.py`, `transform.py`, `operators/tracking_receiver.py`, their tests and `tests/freed_test_sender.py` deleted; kept in git history). The iOS side is removed too (task 1.4.2a): `FreeDPacketEncoder.swift`, the Euler `TrackingPose.swift` and `FreeDPacketEncoderTests.swift` are deleted, and the app builds VCP `POSE` instead. Still to do: `legacy/DesktopReceiver/src/protocol/FreeDParser.cpp` (ARC-006 retirement). |
| PR-FD-002, PR-OTIO-001 | Not started | Optional, T4. |

### §7 Cross-platform

| ID | Status | Evidence / gap |
|---|---|---|
| XP-001 | Done | CI run `35958600428`: `wheels` (manylinux 2_28, Windows, macOS arm64) and `extension` (`--split-platforms`) green. |
| XP-002 | Partial | CI `blender-smoke` green on Linux, Windows, macOS: `VCAM_NATIVE_OK 0.1.0` in each (run `35958600428`). That's the import smoke test. The fake iPhone exists (`native/vcam-fake-iphone`, task 1.2.7): a real binary that pairs, sets up a session and streams `testdata/motion/scripted.bin`, tested against `ControlServer` (`native/vcam-fake-iphone/tests/fake_iphone.rs`). A one-off local run drove `vcam_native.Session` in headless Blender 5.2.2 end to end (log iteration 31). Task 1.3.5: the `blender-smoke` job now builds the fake iPhone (`cargo build --release --locked -p vcam-fake-iphone`, `.github/workflows/ci.yml:176-181`) and runs `tests/blender/addon_apply.py` (camera `matrix_basis` and `matrix_world` at all 5 scripted keyposes under a translated/rotated/scaled rig, max error 1.8e-7, plus scripted controls over the wire), `addon_panel.py` and `addon_robust.py` headless on Linux, Windows and macOS (`ci.yml:220-230`). Verified locally by executing the job's own `run:` blocks from `ci.yml` on macOS (all pass; a wrong-translation mutation makes the step exit 1). Stays Partial until the job has run green on GitHub (awaiting push). |
| XP-003 | Not started | S-2 found that the recommended encoders link statically (turbojpeg) or are OS frameworks; no production dependency yet. |
| XP-004 | Not started | S-3a (SRS §13.3): the linker-ad-hoc-signed `.so` loads when installed from a quarantined zip through Blender. A quarantined `.so` can hit a Gatekeeper prompt and denial, so Developer ID signing + notarization is still required (owner credentials). |
| XP-005/006 | Not started | |

### §8–§10

| ID | Status | Evidence / gap |
|---|---|---|
| NET-001 | Partial | Pure-Rust dual DNS-SD via pinned mdns-sd 0.21.4 (`native/vcam-net/src/discovery.rs:19`, `:78`, task 1.2.3): actual TCP/UDP ports, vcp/blend/host/tcp/udp TXT, bind-scoped addresses, updates and stop/drop withdrawal. `ControlServer::advertise`/`discovery_error` (`control.rs:203`, `:226`) own the daemon while listening; no Bonjour/Avahi runtime dependency. Real multicast smoke plus independent macOS Bonjour resolution verified both services, updates, IPv4/explicit-loopback address selection, withdrawal and re-enable (`docs/LOOP_LOG.md`, iteration 26). Still to do: Python/Blender wiring (1.2.5), iOS discovery (1.4.3), Windows/Linux runtime verification. |
| NET-002 | Partial | Rust `UdpReceiver` (task 1.2.1): one datagram per pose, never retransmitted; newest `seq` wins via `SeqFilter`; reordered or replayed poses counted as stale (`native/vcam-net/tests/udp_receiver.rs`, 6 loopback tests). Exposed to Blender as `vcam_native.Session.latest_pose()` (1.2.5a); pose apply is 1.3.1/1.3.2. |
| NET-003 | Partial | Host side done (task 1.2.4). Authenticated CLOCK requests at 1 Hz on a monotonic host clock; a request is recorded only once sent (`native/vcam-net/src/udp.rs:262-273`; timing test `native/vcam-net/tests/udp_receiver.rs:209`). `vcam-protocol` `ClockEstimator` (`native/vcam-protocol/src/clock.rs:51-123`): replies must match one of the last 4 requests, are consumed once, must arrive < 2 s, and impossible timestamps are rejected. Offset/δ come from the lowest-δ sample of an 8-sample window (newest on ties); jitter is the integer RMS of θ. It matches `testdata/vcp/clock_sync.json` (`native/vcam-protocol/tests/golden.rs:276`; 4 mutations caught). Per-session estimate and rejected-reply count are in `ReceiverStats::clock`/`clock_rejected` (`udp.rs:221`, `:303`). The host clock is exposed as `host_clock_ns()` (`udp.rs:435`, `control.rs:247`) for `ClockEstimate::host_time_ns` (`clock.rs:44`). A real loopback test with a device clock 3.5 s ahead estimates the offset within 5 ms, rejects replayed/unrequested replies, and resets on a new session (`udp_receiver.rs:246`, 10/10 runs). Still to do: Python exposure (1.2.5); device-side replies (1.4.x); O-1 device clock check. |
| NET-004 | Partial | Host reconnect replaces the authenticated TCP/UDP session, closes old TCP, rejects old keys and resets samples/sequence filters (`native/vcam-net/tests/control_server.rs:372`); reconnect after the 10-second idle timeout is covered at `:210`. STATUS (2 Hz/on change) and CLOCK (1 Hz) now follow the latest authenticated UDP source; forged traffic cannot redirect them, and replacement/revocation clears routing and outbound state (`native/vcam-net/tests/udp_receiver.rs:313`, task 1.2.2b.2b). `.blend` reload (task 1.3.4): the session, its sockets and its poll timer survive `open_mainfile`/`read_homefile` (`tests/blender/addon_robust.py:117`); the file name is re-advertised (`core/session.py:228`). Still to do: device-side retry and 3 s reconnect measurement (1.4.x). |
| NET-VID-* | Not started | S-2a/b/c (SRS §13.2), macOS: Stage A JPEG 540p q80 is about 1 ms at 9–13 Mbit/s @ 30 fps. Stage B VideoToolbox H.264 720p is 3.6/5.6 ms (med/p95). The OpenH264 fallback at 720p is about 3/5.5 ms plus 2–3 ms conversion. The software-fallback licensing is an open owner decision. Production encoder code comes in Phases 2–3. |
| LNS-* | Not started | |
| NFR-LAT-*, NFR-PERF-* | Not started | No latency measurements. Render/readback costs are measured by S-1 (SRS §13.1). |
| NFR-REL-001 | Partial | Rust parsers never panic on network data (clippy-enforced no `unwrap`; prefix/flip/random sweeps; fuzz targets). The receiver thread survives bad datagrams and socket errors. PyO3 boundary: every `Session` method runs under `guard` (`native/vcam-py/src/lib.rs:23`), which turns a panic into `vcam_native.NativeError`, a `RuntimeError` subclass (`:15`) that `except Exception` catches, instead of PyO3's `PanicException` (a `BaseException`). Verified in Blender through `_panic_probe` (`:73`; `tests/blender/session_native.py`), and the session survives it; removing the guard fails the test. Caller mistakes raise `ValueError` (`io_err`, `:88`). Still to do: add-on-level status messages (1.3.x). |
| NFR-REL-002 | Partial | `ControlServer::stop()` joins TCP/UDP workers, clears keys/samples and withdraws owned DNS-SD (`native/vcam-net/src/control.rs:288`, `discovery.rs:49`). DNS-SD shutdown acknowledgement is bounded to 500 ms and errors are returned; stopped servers cannot advertise (`native/vcam-net/tests/control_server.rs:496`). Real multicast smoke measured 38 ms stop and verified withdrawal and re-enable (iteration 26). From Python: `Session.stop()` (`native/vcam-py/src/lib.rs:155`) is idempotent, releases the GIL, and took 50 ms in headless Blender. The TCP port then refuses connections and a new `Session` binds the same TCP and UDP ports (`tests/blender/session_native.py`). The add-on's `unregister()` stops the session and its timer poll first (`BlenderAddOn/__init__.py:24`, `core/session.py:266`). Disabling the add-on with a session running took 84 ms; the port then refuses connections, and re-enabling starts on the same port with the same host ID (`tests/blender/addon_session.py`, task 1.2.5b; 3 mutations caught). |
| NFR-REL-003 | Not started | |
| NFR-SEC-001 | Partial | Rust: SRP-6a, HKDF session keys, constant-time proofs, authenticated UDP, single-use/expiring codes and failed-proof lockout. Pairing keys persist atomically before PAIR_ACCEPT (`native/vcam-net/src/store.rs:37-126`, `control.rs:468-483`; storage-failure/private-file tests `native/vcam-net/tests/control_server.rs:632`, `:766`). Verified TCP sessions own UDP keys, revoked on replacement/disconnect/10-second idle expiry/shutdown (`udp.rs:166`, `:330`, `:354`, `:429`). Outbound STATUS/CLOCK uses those keys and only an authenticated source; stale-session status updates are refused (`udp.rs:218`, `:375`; tests `native/vcam-net/tests/udp_receiver.rs:246`, `:313`, 1.2.2b.2b). Blender keeps the pairings and a stable host ID in the extension's user directory (`BlenderAddOn/core/session.py:59`, `:85`, task 1.2.5b); the ID file is mode 0600, and a damaged file is rejected, not replaced. Still to do: iOS Keychain (1.4.3). |
| NFR-SEC-002 | Not started | |
| NFR-SEC-003 | Partial | Host side: Blender listens only while a session is running. Enabling the add-on opens no ports; `vcam.session_start`/`vcam.session_stop` (`BlenderAddOn/operators/session.py`) and disabling the add-on control both ports (`tests/blender/addon_session.py`, task 1.2.5b). No analytics code exists. iOS: the camera usage string (1.4.1) and the local-network usage string plus the single browsed Bonjour type `_vcam-ctl._tcp` (`VCamIOS/Info.plist`, task 1.4.3a). |
| NFR-QA-001 | Partial | One git repo. `xcodebuild test`, `cargo test` (in `native/`), and headless Blender tests of the installed extension (`tests/blender/smoke_native.py`, `session_native.py`, `addon_session.py`, `addon_apply.py`, `addon_panel.py`, `addon_robust.py`) all run. All six are in the CI `blender-smoke` job on 3 OSes; the three that need the fake iPhone use a release build made in that job (task 1.3.5). Only the first import smoke has run there so far; the rest await a push. There's no single `blender --background … tests` runner for add-on logic yet. |
| NFR-QA-002 | Partial | `ci.yml` runs fmt, clippy (`-D warnings`), and test on 3 OSes, plus Python tests, headless Blender on 3 OSes (now including the fake-iPhone integration tests, task 1.3.5), iOS unit tests, and (new) a `fuzz` job: nightly + `cargo-fuzz 0.13.2`, 60 s per target, seeded from `testdata/`, artifacts uploaded on failure. The first GitHub run failed only on clippy (fixed locally); the fuzz job hasn't run yet (awaiting push). |
| NFR-QA-003 | Partial | `testdata/` holds VCP message, receive-rule, pairing/session, clock-sync (`vcp/clock_sync.json`, task 1.2.4), coordinate (task 1.1.2) scripted-motion (`motion/scripted.{json,bin}`, task 1.2.7: 390 frames at 60 Hz, 5 keyposes with `matrix_world`, self-checked look directions) and rig vectors (`rig/rig_cases.json`, tasks 1.3.2a/b: 11 cases, including the expected tilt/dolly/crane keyposes of the scripted-control fake-iPhone run, computed with rotation matrices independently of the quaternion implementation). Consumed by Rust (`vcam-protocol/tests/`) and Swift (`VCamIOSTests/VCPGoldenTests.swift`, bundled via a `testdata` folder reference). Clock sync is host-only, so Swift doesn't consume `clock_sync.json`. Python consumes `rig_cases.json` and `freshness.json` (`BlenderAddOn/tests/test_rig.py`); there are no fragment-reassembly vectors yet (Phase 2). CI checks the vectors are up to date. |
| NFR-QA-004 | Partial | Enforced by workspace lints: `unsafe_code` deny plus `clippy::undocumented_unsafe_blocks` deny (`native/Cargo.toml:22-32`). Checked with a throwaway probe: clippy rejected an uncommented `unsafe` block. |

---

## Next milestone

**Phase 0 exit:** single repo; `DesktopReceiver/` moved to `legacy/`; Rust workspace with a PyO3 module importing inside Blender 5.2 on all three OSes via CI; spikes S-1 (render/readback), S-2 (encoders), and S-3 (macOS loading) answered.
